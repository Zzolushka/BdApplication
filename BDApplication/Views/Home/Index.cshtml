@model IEnumerable<BDApplication.Models.Sketch>

@{
    ViewBag.Title = "Home Page";
}

<header class="jumbotron my-4">
    <h1 class="display-3">Рисовальщик</h1>
    <p class="lead">Добрбро пожаловть</p>
    <a href="#" class="btn btn-primary btn-lg">Зарегистрироваться</a>
</header>

<!-- Page Features -->
@*<div class="row text-center">
    @foreach (var item in Model)
    {
        <div class="col-lg-3 col-md-6 mb-4">
            <div class="card">
                <img class="card-img-top" src="@item.SketchPhoto" alt="">
                <div class="card-body">
                    <h4 class="card-title">Card title</h4>
                    <p class="card-text">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Sapiente esse necessitatibus neque.</p>
                </div>
                <div class="card-footer">
                    <a href="#" class="btn btn-primary">Find Out More!</a>
                </div>
            </div>
        </div>
    }
</div>*@